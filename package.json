{
  "name": "drboom-seaquell",
  "version": "0.1.1",
  "description": "An extension for drboom to catch errors from seaquell.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nathan Fritz",
  "license": "MIT",
  "dependencies": {
    "boom": "^3.0.0",
    "seaquell": "^0.6.0"
  }
}
